Current task: "{self.task_text}"
Task-specific official/baseline hint: "{self.task_prompt}"

Structured competition state:
{self.competition_state}

Current visible objects (object_id values are the only valid IDs for object actions):
{self.visiable_objects_info}

NPC reply, if any: "{self.npc_reply}"
NPC task hints, if any: {self.npc_subject}

Previous environment action result: {self.action_res}
Previous official task-service response: {self.apply_resp}
Recent actions: {self.action_histories}

Choose exactly one next action. Base it only on current evidence and the official action schemas.
Include expected_change so the next observation can verify success. Keep think short and factual.
